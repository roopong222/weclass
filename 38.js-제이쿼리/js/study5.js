$(document).ready(function(){
    // $(`.box`).animate({width:`400px`,height:`400px`},1000)
    // $(`.box`).animate({width:`400px`},1000).animate({height:400},1000).clearQueue()
    // $(`.box`).animate({width:`400px`},1000).animate({height:400},1000).stop()
    // $(`.box`).animate({width:`400px`},1000).delay(1000).animate({height:400},1000)

    
    // let b1 = $(`.b1`).offset().top
    // let b2 = $(`.b2`).offset().top
    // let b3 = $(`.b3`).offset().top
    // let b4 = $(`.b4`).offset().top

    // $(`html,body`).animate({scrollTop:b2},1000)
})